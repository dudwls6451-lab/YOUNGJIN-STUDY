# v11.60.46 · 스터디그룹 문제 만들기 모드 표시 핫픽스

기준: v11.60.44 또는 v11.60.45

## 원인
v11.60.45는 새 Question Lab UI를 `#studyGroupCard`가 없을 때만 생성했습니다. 기존 스터디그룹 DOM이 이미 마운트된 상태에서는 기존 화면을 그대로 사용하면서 `문제 풀기 / 문제 만들기 / 문제 목록` 영역을 추가하지 않는 경로가 있었습니다.

## 수정
- 기존 스터디그룹 화면이 이미 존재해도 Question Lab을 강제로 업그레이드합니다.
- `문제 만들기` 탭이 없으면 Question Lab 전체 영역을 삽입합니다.
- 부분 적용으로 섹션만 있고 탭이 없는 경우도 재구성합니다.
- 상단 조회 날짜 영역에 `문제 풀기`, `＋ 문제 만들기` 바로가기 버튼을 추가했습니다.
- 기존 목표 종류 셀렉트에도 `문제 만들기`가 없으면 자동 추가합니다.
- 캐시 키를 v11.60.46으로 올렸습니다.

## 적용
1. ZIP 내용을 저장소 루트에 덮어씁니다.
2. v11.60.45 SQL을 아직 실행하지 않았다면 `supabase/V11.60.45_STUDY_GROUP_QUESTION_LAB.sql`을 Supabase SQL Editor에서 1회 실행합니다.
3. 배포 후 브라우저에서 강력 새로고침(Ctrl+Shift+R)을 한 번 수행합니다.

정상 적용 시 스터디그룹 상세 상단의 조회 날짜 줄에 `문제 풀기`, `＋ 문제 만들기` 버튼이 보이고, 바로 아래에 `문제 풀기 / 문제 만들기 / 문제 목록` 탭이 표시됩니다.
