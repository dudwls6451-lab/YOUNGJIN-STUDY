# PilotBank v11.60.13 — 관리자 인라인 문제 수정 + 오류보고 전체 삭제

기준: v11.60.12 한국어 전수 재점검 + 오류보고 34건 반영 패치.

## 관리자 문제 바로 수정
- 문제 풀이 화면 우측 액션 영역에 관리자에게만 `✎ 문제 수정` 버튼이 표시됩니다.
- 문제 지문, 현재 선택지 전체, 정답, 해설을 한 화면에서 수정할 수 있습니다.
- 저장 즉시 현재 브라우저의 문제/선택지/정답/해설에 반영됩니다.
- 수정본은 `public.question_overrides`에 저장되며 승인된 회원이 문제은행을 다음에 불러올 때 정적 JSON 위에 자동으로 덮어씁니다.
- 일반 회원에게 수정 버튼은 표시되지 않으며 RLS에서도 관리자만 insert/update/delete 가능합니다.
- 정적 JSON 원본은 직접 변경하지 않으므로 관리자가 실수로 원본 파일을 훼손하지 않습니다.

## 관리자 보고된 오류 전체 삭제
- 관리자 `보고된 오류` 화면에 `전체 삭제` 버튼을 추가했습니다.
- 삭제 전 확인창이 표시됩니다.
- `pilotbank_admin_clear_error_reports()` RPC가 서버의 오류보고 전체를 삭제합니다.
- 삭제된 보고가 사용자 화면에서 다시 전송대기로 살아나는 것을 막기 위해 해당 보고와 연결된 `user_question_progress.error_reported` 관련 필드도 함께 초기화합니다.

## 배포 시 필수
`supabase/V11.60.13_ADMIN_INLINE_EDIT_ERROR_CLEAR.sql`을 Supabase SQL Editor에서 1회 실행해야 합니다. SQL은 재실행 가능하도록 작성되어 있습니다.

## 적용 범위
관리자 인라인 편집은 메인 문제은행의 일반 학습/시험/오답복습/이론 쪽지시험에서 공통으로 사용하는 문제 화면에 적용됩니다. 주간랭킹전은 서버 고정 출제 풀/서버 채점 구조이므로 이번 인라인 편집 대상에 포함하지 않았습니다.
