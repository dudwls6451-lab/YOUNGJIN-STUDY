# PilotBank v11.60.47 · 스터디그룹 사용자 카테고리

기준 UI: v11.60.46 Study Group Question Lab mount hotfix
DB 선행조건: v11.60.45 Study Group Question Lab SQL

## 추가 기능

- 문제 만들기 화면에서 그룹원이 직접 카테고리 생성
- 그룹 단위 카테고리 공유: 한 그룹원이 만든 카테고리를 같은 그룹의 다른 출제자도 선택 가능
- 같은 그룹 내 카테고리 이름은 대소문자/앞뒤 공백을 무시해 중복 생성 방지
- 문제 등록 시 카테고리 선택 또는 `미분류` 선택 가능
- 문제 풀이 / 문제 목록에서 카테고리 필터 제공
- 문제 카드에 카테고리 배지 표시
- 기존 과목/태그 필드는 별도로 유지
- 새 문제 등록 후 해당 카테고리로 자동 필터하여 방금 만든 문제 확인 가능

## DB 적용

v11.60.45 SQL을 이미 실행했다면 아래 SQL만 1회 실행합니다.

`supabase/V11.60.47_STUDY_GROUP_QUESTION_CATEGORIES.sql`

v11.60.45 SQL을 아직 실행하지 않았다면 먼저 `V11.60.45_STUDY_GROUP_QUESTION_LAB.sql`을 실행한 뒤 v11.60.47 SQL을 실행합니다.
