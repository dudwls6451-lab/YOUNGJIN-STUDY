# PilotBank v11.60.42 · Reference Annotation Sync

Base: v11.60.41

## Added
- 고정익항공기를 위한 운항기술기준 `원문 학습`에 Aviwiki형 형광펜/자유필기 도구 추가.
- ICAO Doc 8168 Vol. I 참고서 페이지에 Aviwiki형 형광펜/자유필기 도구 추가.
- 형광펜 4색(노랑/초록/분홍/파랑), 선택 지우기.
- 자유필기: 이동/펜/마커/지우개, 색상, 굵기, 되돌리기, 전체 지우기.
- 편집 즉시 로컬 저장 + 로그인 계정의 Supabase `pilotbank_study_annotations`에 자동 upsert.
- 문서/조항/페이지별 분리 저장. 다른 기기에서 같은 계정으로 열면 서버 상태를 다시 불러옴.
- 탭/앱이 다시 활성화될 때 FSR/8168 annotation을 서버에서 재확인.
- Aviwiki 기존 localStorage 형광펜/자유필기도 동일 테이블에 동기화하도록 확장.
- Aviwiki 서버 row가 아직 없고 로컬 annotation이 있으면 해당 이론 섹션을 열 때 최초 1회 자동 업로드(점진적 마이그레이션).
- 로컬 수정 시각이 서버보다 최신이면 로컬 상태를 서버에 우선 반영해 최근 오프라인 편집의 덮어쓰기를 줄임.

## Required Supabase migration
웹 패치 배포 전에 `V11.60.42_REFERENCE_ANNOTATIONS_SYNC.sql`을 Supabase SQL Editor에서 1회 실행한다.

테이블은 RLS가 활성화되며 authenticated 사용자는 자신의 `user_id` row만 SELECT/INSERT/UPDATE/DELETE할 수 있다. anon 권한은 제거한다.

## Storage model
`pilotbank_study_annotations` primary key:
- `user_id`
- `surface_key`: `aviwiki` / `fsr` / `icao8168`
- `content_key`: Aviwiki section id, FSR 조항 code, ICAO page+view mode

payload에는 editable highlight offsets와 freehand strokes가 들어간다. FSR/8168의 freehand 좌표는 surface normalized 좌표로 저장해 화면 크기가 달라도 비례 재생한다.

## Notes
- Supabase migration이 아직 적용되지 않은 환경에서는 기능이 깨지지 않고 해당 기기의 localStorage에 계속 저장하며 툴바에 `로컬 저장 · 서버 설정 필요`를 표시한다.
- FSR의 문제 제작(authoring) 모드에서는 원문 드래그가 문제 발췌 기능에 사용되므로 annotation toolbar를 표시하지 않는다. 일반 `원문 학습`에서만 표시한다.
- ICAO 8168은 한국어/영문/병기 모드의 본문 텍스트 구성이 달라 각 view mode별 annotation key를 사용한다.
