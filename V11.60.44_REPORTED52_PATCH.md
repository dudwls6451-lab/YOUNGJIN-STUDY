# PilotBank v11.60.44 · 보고된 오류 52건 + 오류 UI/전체삭제 hotfix

기준 버전: **v11.60.43**

## 반영 내용

- 오늘 전달된 오류 52건 중, v11.60.12에서 이미 원본대조 완료된 34건을 유지하고 새 18건을 추가 검수·교정했습니다.
- K-AIM-923의 깨진 V 신호/선택지를 원본에서 복원했습니다.
- K-AIM-102는 교재 정답표(C)와 교재 자체 해설이 충돌하여 해설·운용조건에 맞는 D로 교정했습니다.
- K-AIM-175와 WX-SU6-007처럼 원문이 단일정답형으로 성립하지 않거나 지나치게 모호한 문항은 학습개념을 유지하면서 단일정답형으로 정리했습니다.
- FLIGHT-THEORY-218에는 원본 Fowler flap 그림을 추출해 연결했습니다.
- 보고된 오류/오류 보고 화면을 다크모드 카드·입력창·글자색으로 통일했습니다.
- 관리자 `전체 삭제`에서 발생하던 `DELETE requires a WHERE clause` 오류를 수정했습니다.

## 적용

1. ZIP을 저장소 루트에 그대로 덮어씁니다.
2. Supabase SQL Editor에서 `supabase/V11.60.44_REPORTED_ERRORS_HOTFIX.sql`을 1회 실행합니다.
3. 배포 후 강력 새로고침/캐시 갱신을 합니다.

DB SQL은 **전체삭제 hotfix에 필요**합니다. 문제 데이터/UI 교정은 정적 파일 덮어쓰기만으로 반영됩니다.
