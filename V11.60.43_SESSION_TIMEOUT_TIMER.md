# v11.60.43 · 세션 자동 로그아웃 타이머

기준: v11.60.42

## 변경 사항

- 자동 로그아웃 기준을 **30분 무동작 → 60분 무동작**으로 변경했습니다.
- 로그인 후 상단 메뉴에 `자동 로그아웃 60:00 · 연장` 컨트롤을 표시합니다.
- 사용자가 계속 조작하는 동안에는 표시값을 `60:00`으로 유지합니다.
- 마지막 화면 동작 후 **1분이 지나면** 카운트다운 표시가 시작됩니다.
  - 실제 자동 로그아웃 시점은 마지막 동작으로부터 정확히 60분입니다.
  - 따라서 카운트다운이 처음 움직이는 시점에는 약 `59:00`이 표시됩니다.
- 클릭, 키보드 입력, 포인터/터치, 스크롤이 발생하면 무동작 시간이 즉시 초기화되어 `60:00`으로 돌아갑니다.
- `연장` 버튼을 누르면 남은 시간이 즉시 60분으로 초기화되고 잠시 `연장됨` 상태를 표시합니다.
- 백그라운드 탭에서 타이머가 브라우저에 의해 지연되더라도 다시 화면으로 돌아오면 실제 경과시간을 기준으로 만료 여부를 재검사합니다.
- 자동 로그아웃 후 로그인 화면 안내 문구를 `1시간 동안 활동이 없어 자동 로그아웃되었습니다.`로 변경했습니다.

## 적용 파일

- `index.html`
- `assets/auth-v11-11.js`

DB/Supabase SQL 변경은 없습니다.
