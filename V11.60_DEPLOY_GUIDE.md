# PilotBank v11.60.0 배포 순서

## 1. DB
Supabase SQL Editor에서 `supabase/V11.60_FULL.sql`을 실행합니다. 기존 v11.59.x 스터디그룹 데이터는 삭제하지 않습니다.
이미 `V11.59.3_STUDY_GROUP_FULL_LATEST.sql`까지 실행했다면 `supabase/V11.60_COMMUNITY_RANKING_PUSH.sql`만 실행해도 됩니다.

## 2. GitHub Pages
`github-question-bank-v11.60.0-push-announcements-weekly-ranking-patch.zip` 내용을 저장소 루트에 덮어쓰고 Commit / Push 합니다.

## 3. Edge Function secrets
별도로 제공되는 `V11.60_PUSH_SECRETS_DO_NOT_COMMIT.txt`는 절대로 GitHub에 올리지 마세요.
그 파일의 값을 이용해 Supabase CLI에서 설정합니다.

```bash
supabase login
supabase link --project-ref YOUR_PROJECT_REF
supabase secrets set VAPID_PUBLIC_KEY="SECRET_FILE_VALUE" VAPID_PRIVATE_KEY="SECRET_FILE_VALUE" VAPID_SUBJECT="mailto:본인이메일@example.com" CRON_SECRET="SECRET_FILE_VALUE"
```

## 4. Edge Functions 배포
```bash
supabase functions deploy pilotbank-push-worker --no-verify-jwt
supabase functions deploy pilotbank-weekly-ranking --no-verify-jwt
```
Hosted Edge Functions의 기본 project secrets인 `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`를 사용하도록 작성되어 있습니다.

## 5. Cron
`supabase/V11.60_CRON_SETUP_TEMPLATE.sql`에서 아래 세 값을 실제 값으로 바꿉니다.
- `YOUR_PROJECT_REF`
- `YOUR_PUBLISHABLE_KEY`
- `YOUR_CRON_SECRET` — 별도 secret 파일의 CRON_SECRET 값

그 뒤 SQL Editor에서 실행합니다.
- Push worker: 매시간 5분 — 24시간 미접속 확인 + 미발송 공지 재시도
- Weekly ranking worker: 매시간 10분 — 현재 KST 주차 랭킹전 보장 + 시간초과 응시 자동 마감
- KST 기준 월요일 00:10 첫 hourly run에서 새 주간랭킹전이 생성됩니다.
- Cron이 아직 설정되지 않았더라도 승인 회원이 주간랭킹전 화면을 열면 현재 주차 round를 즉시 만드는 fallback이 있습니다.

## 6. 스마트폰 / iPad 알림
Android/데스크톱: 공지사항 화면 → `푸시 알림 켜기`.
iPhone/iPad: Safari 공유 버튼 → `홈 화면에 추가` → 홈 화면의 PilotBank 실행 → 공지사항 → `푸시 알림 켜기`.

## 주간랭킹전 규칙
- 4,580개 필터 통과 텍스트 문항 풀에서 매주 서버가 동일 50문항 선정
- 그림/figure 의존 문항, OCR 재검토 플래그 문항, 모의고사 섹션 제외
- 50분 제한, 1인 1회
- 시작시각은 DB에 기록되고 점수는 Edge Function에서 채점
- 제출 후에만 랭킹/댓글 열람 가능
- 본인 오답, 정답, 해설 열람 가능

## 보안 메모
기존 문제은행이 정적 JSON/JS 기반이므로 개발자도구로 원본 문제 데이터를 보는 것까지 막는 온라인 감독시험 수준의 anti-cheat 시스템은 아닙니다. 다만 랭킹전 문제 선정, 시작시각, 제출 상태, 서버 채점 및 순위는 Supabase 측에서 관리합니다.
