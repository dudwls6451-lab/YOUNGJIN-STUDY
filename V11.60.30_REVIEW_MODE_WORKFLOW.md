# PilotBank v11.60.30 · 부관리자 / 검수 모드 / 2-role 배포 승인

## 적용 순서
1. **먼저** Supabase SQL Editor에서 `supabase/V11.60.30_REVIEWER_WORKFLOW.sql` 전체를 실행합니다.
2. 그 다음 이 ZIP의 나머지 파일을 **v11.60.29 위에 폴더 구조 그대로 덮어씁니다.**
3. 관리자/부관리자는 새로고침 후 상단 `검수 모드`, `배포 확인` 메뉴를 사용할 수 있습니다.

## 역할
- 관리자: 기존 관리자 기능을 유지하며 회원 관리 화면에서 승인 회원을 `부관리자`로 지정/해제할 수 있습니다.
- 부관리자: 기존 일반 관리자 권한은 받지 않고, `검수 모드`와 `배포 확인`만 사용할 수 있습니다.
- 배포 승인 요건: **관리자 1명 동의 + 부관리자 1명 동의**가 모두 있어야 자동 배포됩니다.

## 검수 모드
- 문제: 전체 교재 문항을 교재별/검색으로 자유 조회. 선택지는 클릭할 수 없고 정답/해설/그림을 바로 확인합니다.
- 이론: 앱에 등록된 모든 이론 과정을 교재별/단계별로 자유 조회. 잠금, 진도, 쪽지시험, 채점이 없습니다.
- 검수 화면 이용은 오답/점수/진도/학습시간에 기록되지 않습니다.

## 수정안과 배포
- 문제 수정안: 지문, 선택지, 정답, 해설을 편집 후 `배포안 제출`.
- 이론 수정안: 단계 제목, 단원 제목, `sections` 구조, 시험 직전 핵심을 편집 후 제출.
- 제출 직후 실제 문제/이론에는 적용되지 않습니다.
- `배포 확인`에서 관리자/부관리자의 동의를 각각 받아야 합니다.
- 두 역할의 동의가 모두 모이면:
  - 문제 → 기존 `question_overrides`에 배포되어 기존 앱 로드 경로로 전체 회원에게 적용됩니다.
  - 이론 → 새 `theory_overrides`에 배포되며 v11.60.30 런타임이 이론 JSON 로드 시 자동 병합합니다.
- 한 명이라도 반려하면 해당 수정안은 배포되지 않으며 새 수정안을 다시 제출해야 합니다.

## 데이터 안전
- 기존 문제 JSON, 이론 JSON, 회원 학습기록은 삭제/변경하지 않습니다.
- `profiles`에 `is_subadmin` 컬럼만 추가합니다.
- 검수 초안/승인 기록은 별도 테이블에 보존됩니다.
- 정적 원본 JSON이 아닌 서버 override 방식이므로 승인 전 수정안이 일반 사용자에게 노출되지 않습니다.
