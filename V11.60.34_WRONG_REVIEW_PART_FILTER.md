# v11.60.34 · 교재별 오답 복습 파트 필터

Base: v11.60.33

- 기존 교재별 오답 복습의 `누적 오답 횟수` 필터 유지
- 교재 선택 후 `파트` 선택 추가
- 선택한 파트에 여러 subunit이 있으면 `세부 파트` 선택 추가
- `전체 파트` 선택 시 기존 방식과 동일
- 현재 조건에 해당하는 오답 수를 실시간 표시
- 선택한 파트 조건은 결과 화면의 `다시 풀기`에서도 유지
- 점수/오답 누적 정책은 기존 오답 복습과 동일
- DB/SQL 변경 없음
