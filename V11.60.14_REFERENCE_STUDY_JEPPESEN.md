# v11.60.14 · 참고서 학습 / Jeppesen Airway Manual

기준: v11.60.13 관리자 인라인 문제 수정 + 오류보고 전체삭제 원샷 패치.

## 추가 기능
- 홈 학습 모드에 `참고서 학습` 추가.
- 빠른 이동 메뉴에 `참고서` 추가.
- 참고서 목록에 `Jeppesen Airway Manual` 추가.
- Jeppesen Airway Manual 안에서 `문제 풀이` / `이론 학습` 제공.

## 데이터
- 이론: `Jeppesen_Airway_Manual_Glossary_설명_한국어번역.txt` 기반.
- 문제: `Jeppesen_Airway_Manual_Glossary_용어별_2문항_문제은행.txt` 기반.
- 고유 용어 443개.
- 4지선다 886문항(각 용어당 설명→용어 1문항 + 용어→설명 1문항).
- 이론 과정: 알파벳 순, 단계당 최대 20개 용어로 묶은 33단계.
- 각 이론 단계 쪽지시험: 해당 단계 문항 중 최대 10문항 무작위, 80% 이상 합격.

## 학습시간
기존 `PilotBankStudyTime` 측정 경로를 그대로 사용한다.
- Jeppesen 문제 풀이: 기존 quiz-active 문제풀이 화면이므로 학습시간에 포함.
- Jeppesen 이론 학습: 기존 theoryCard 리더 화면이므로 학습시간에 포함.
- 참고서 선택 메뉴에서 머무는 시간은 학습시간에 포함하지 않음.
- 기존 10분 미조작 / 백그라운드 제외 규칙 유지.

## 기존 기능과의 관계
- Jeppesen 문항은 일반 `자유학습모드`에는 섞이지 않도록 참고서 전용 과목으로 제외 처리.
- 오답/즐겨찾기/오류신고/관리자 인라인 수정 등 기존 문제 기록 구조를 그대로 사용.
- Supabase 추가 SQL은 필요 없음.
